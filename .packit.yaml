---
jobs:
  - job: tests
    trigger: pull_request
    branch: main
    targets:
      - fedora-rawhide
      - centos-stream-10-x86_64
    skip_build: true
    tf_extra_params:
      environments:
        - tmt:
            context:
              target_PR_branch: main

